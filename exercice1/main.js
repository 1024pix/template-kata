/*
* The function should add a grade property for each user
* The grades should be as such :
* - if note is below 10, the grade should be "not so good"
* - if note is between 10 and 15, the grade should be "good"
* - if note is above 15, the grade should be "very good"
* */
const attributeGrade = () => {
  return {};
};

export { attributeGrade }
